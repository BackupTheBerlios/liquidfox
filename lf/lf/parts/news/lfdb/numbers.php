<?
$tablename=$lfconf_db['prefix']."numbers";
$column=0; // Don't touch

	//id

$column++;
$db[$column]['name']  = "id";
$db[$column]['type']  = "int(11)";

$dbid = "id";
	//how

$column++;
$db[$column]['name']  = "how";
$db[$column]['type']  = "varchar(20)";

	//name

$column++;
$db[$column]['name']  = "name";
$db[$column]['type']  = "varchar(20)";

	//add

$column++;
$db[$column]['name']  = "add";
$db[$column]['type']  = "varchar(20)";

	//mail

$column++;
$db[$column]['name']  = "mail";
$db[$column]['type']  = "varchar(20)";

	//price

$column++;
$db[$column]['name']  = "price";
$db[$column]['type']  = "varchar(20)";

	//money

$column++;
$db[$column]['name']  = "money";
$db[$column]['type']  = "varchar(20)";

	//phone

$column++;
$db[$column]['name']  = "phone";
$db[$column]['type']  = "varchar(20)";

	//time

$column++;
$db[$column]['name']  = "time";
$db[$column]['type']  = "varchar(20)";

	//time_updown

$column++;
$db[$column]['name']  = "time_updown";
$db[$column]['type']  = "varchar(20)";

	//add_date

$column++;
$db[$column]['name']  = "add_date";
$db[$column]['type']  = "varchar(20)";

	//area

$column++;
$db[$column]['name']  = "area";
$db[$column]['type']  = "varchar(20)";

	//number

$column++;
$db[$column]['name']  = "number";
$db[$column]['type']  = "varchar(20)";

	//do_2

$column++;
$db[$column]['name']  = "do_2";
$db[$column]['type']  = "varchar(20)";

	//cell

$column++;
$db[$column]['name']  = "cell";
$db[$column]['type']  = "varchar(15)";

	//city

$column++;
$db[$column]['name']  = "city";
$db[$column]['type']  = "varchar(20)";

	//money_type

$column++;
$db[$column]['name']  = "money_type";
$db[$column]['type']  = "varchar(10)";

	//maam

$column++;
$db[$column]['name']  = "maam";
$db[$column]['type']  = "varchar(10)";

	//addons

$column++;
$db[$column]['name']  = "addons";
$db[$column]['type']  = "varchar(200)";

?>